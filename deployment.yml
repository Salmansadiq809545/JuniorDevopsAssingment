---
- hosts: demo
  gather_facts: false
  remote_user: root
  tasks:
    - name: Building The image
      docker_image:
        name: myimg
        source: build
        build:
          path: /home/ubuntu/Project/
        state: present

    - name: Building Container
      docker_container:
        name: c000
        image: myimg
        ports:
          - "8091:8091"
        state: started
